package com.example.androidmusic.ui.home

/**
 * Immutable state rendered by [HomeScreen]. Phase 0 placeholder; the screen is
 * deliberately a pure function of this state so it can be previewed and
 * screenshot-tested without a ViewModel.
 */
data class HomeUiState(
    val title: String,
    val message: String,
)
